# Face-Mask-Detector-Model
The model was trained with Google's Teachable Machine and runs entirely client-side using TensorFlow.js — no frames are uploaded or sent to a server. Detections are rendered as a live event log, styled like a SOC/SIEM console (timestamp, severity level, message, confidence).
